---
pagination:
  data: languages
  alias: lang
  size: 1
  addAllPagesToCollections: true
layout: layout.liquid
tags: indexes
eleventyComputed:
  metadata:
    title: "Taylor Swift {{ 'site.title' | i18n }}"
    titleHtml: "<b>Taylor Swift</b> {{ 'site.title' | i18n }}"
    cta: false
permalink: "{% if lang != 'en' %}/{{ lang }}/{% else %}/{% endif %}"
---
<h2>{{ "songs" | i18n }}</h2>

<div class="song-chooser">
{%- for song in collections.songPages %}
  {%- if song.data.song.language == lang %}
    <a href="{{ song.url }}">
    {% albumArt song.data.song %}
    {{ song.data.song.title }}
    </a>
  {%- endif %}
{%- endfor %}
</div>